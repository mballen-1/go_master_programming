package main

/*func main() {
	file, err := os.Open(path)
	HandleError(err)
	defer file.Close()

	scn := bufio.NewScanner(file)
	for scn.Scan(){
		fmt.Println(scn.Text())
	}

	if err := scn.Err(); err != nil{
		HandleError(err)
	}
}*/
